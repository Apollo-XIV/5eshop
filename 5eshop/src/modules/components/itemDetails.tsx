import {ItemType} from "../hooks/5eItemList";

type Props = {
    selectedItem: ItemType
}

export default function itemDetails({selectedItem}: Props) {
    return <>
        <div>test</div>
    </>
}